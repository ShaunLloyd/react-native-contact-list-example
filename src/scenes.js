import type { Scene } from 'common';
import { ContactsScene } from 'scenes/contacts';
import { AddContactFormScene } from 'scenes/add-contact-form';

export const scenes: Scene[] = [
  ContactsScene,
  AddContactFormScene,
];
